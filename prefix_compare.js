/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
    for(let i = 0; i < strs.length; i++){
        for(let j = 0; j < strs[i].length; ){
            if(){
                //compara o string
                //while strs[i][j] == strs[i+1][j] => stringResult.push(strs[i][j]) 
                //é isso eu acho
            }
        }
    }
};
